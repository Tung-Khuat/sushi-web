import { css } from 'styled-components';

export const AbsCenter = css`
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    margin: auto;
`

export const NoButtonAttributes = css`
    border: none;
    background: transparent;
`